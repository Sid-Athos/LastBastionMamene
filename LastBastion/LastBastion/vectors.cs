using System;
using System.Collections.Generic;
using System.Text;

namespace LastBastion
{
    public class Vectors
    {
        float _posX;
        float _posY;
        

        public Vectors(float x, float y)
        {
            _posX = x;
            _posY = y;
        }

        /**public float Magnitude()
        {
            //return Math.Sqrt((_posX * _posX) - (_posY * _posY));
        }*/
    }
}
